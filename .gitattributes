# Windows 批处理必须是 CRLF，否则 cmd 会报"不是内部或外部命令"
*.bat   text eol=crlf
*.cmd   text eol=crlf
# shell 脚本必须是 LF
*.sh    text eol=lf
*.command text eol=lf
*.desktop text eol=lf
# 二进制
*.png binary
*.ico binary
*.zip binary
